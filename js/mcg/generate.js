Object.assign(MCG.Generate, (function() {

  return {

    infillLinear: function(min, max, context, spacing, angle, parity) {
      context = context || new MCG.Context();
      spacing = spacing || context.p;
      angle = angle || 0;
      parity = parity || 0;

      
    },

  };

})());
